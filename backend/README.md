instrukcja odpalenia projektu w dockerze dla prawdziwych sigm:
w folderze glownym projektu springboot:
- mvn clean package | ./mvnw clean package
- docker-compose up --build

serwer powinien wstac na port 8080